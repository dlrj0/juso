# 현장조사 동선 정리 툴 — 인수인계 문서

> **이 문서 관리 규칙**: 이 프로젝트를 코드로 건드리는 모든 작업(기능 추가, 버그 수정, 리팩토링) 후에는 이 문서를 반드시 최신 상태로 갱신할 것. 특히 "검증됨 / 검증 안 됨", "다음 할 일" 섹션은 작업 전후로 꼭 업데이트.

## 1. 이게 뭔가요

농지조사원(사용자)이 농지정보시스템에서 부적합필지를 추출한 엑셀을, 팜필드(FarmField, 국가 농지전수조사 플랫폼) 현장 입력 전에 **마을 단위로 그룹핑하고 방문 순서를 관리**할 수 있게 해주는 보조 도구. 팜필드/농지정보시스템을 대체하지 않고, 그 앞단의 "오늘 어디부터 갈지"만 도와주는 용도.

- 산출물: `field_route_planner.html` (단일 HTML 파일, React 아님 — vanilla JS + SheetJS)
- 기본 처리 방식: **파일 자체는 서버 전송 없이 브라우저 안에서만 처리**. 이 원칙은 유지하되, 4번째 세션부터 **사용자 동의 하에 좌표 자동계산을 위한 외부 지오코딩 API 호출은 선택적으로 허용**함 (2절 "세션별 변경 이력" 참고). API를 쓰지 않으면 이전과 동일하게 완전히 로컬 처리.

## 2. 세션별 변경 이력 (최신 세션이 위로)

### 12번째 세션 — 카카오 SDK document.write 근본 원인 수정 + 지도 보기 화면 신설

**배경**: 11번째 세션에서 SDK를 동적 `<script>` 삽입으로 로드하도록 바꾼 뒤, 실제 브라우저에서 `Failed to execute 'write' on 'Document': ...asynchronously-loaded external script...` 에러로 마을/필지 좌표 자동계산이 막힘. 사용자가 F12 콘솔 로그를 그대로 붙여줘서 진단함. 1차로 `script.async=false`만 적용했으나 재현됐고, 근본 원인(아래)을 파악해 2차로 다시 고침 — 사용자가 재테스트 결과 마을 좌표 자동계산 성공을 확인함(필지 단위는 호출량이 많아 이번 세션엔 미검증). 이어서 "계산한 좌표를 지도 위 핀으로 보고 싶다"는 요청으로 지도 보기 화면을 신설함.

### 실제로 바꾼 것
1. **`loadKakaoSdkScript()` 근본 수정 — `document.write` 임시 가로채기 방식으로 교체.** 카카오맵 SDK는 `libraries=services` 옵션을 쓸 때 내부적으로 `document.write()`로 Geocoder/Places 라이브러리 파일을 추가 삽입하는데, 이 프로젝트는 페이지 로드가 끝난 뒤(버튼 클릭 시점)에 SDK를 동적으로 넣는 구조라 `document.write()`가 `async` 값과 무관하게 항상 예외를 던짐(브라우저 스펙상 "이미 로드 완료된 문서에서 비동기 스크립트의 document.write 호출"은 금지됨). `script.async=false`만으로는 해결 안 됨을 확인 — SDK 실행 구간 동안만 `document.write`/`document.writeln`을 가로채서, 문서를 지우는 대신 `<head>`에 `<script>` 태그를 직접 추가하는 방식으로 대체. 가로채기로 추가된 하위 스크립트(services 라이브러리 본체)까지 로드 완료된 뒤 Promise가 resolve되도록 함.
2. **지도 보기 화면(`screen-map`) 신설.** 동선(route) 화면에 "🗺️ 지도에서 보기" 버튼 추가 → 클릭 시 계산된 마을 중심좌표(`village.centroid`)와 필지 좌표(`parcel.coord`)를 카카오맵(`kakao.maps.Map`) 위에 마커로 표시. 마을은 기본 마커, 필지는 주황색 원형 커스텀 마커(필지 순서가 계산돼 있으면 InfoWindow에 순번도 표시)로 구분. 지도 인스턴스(`_kakaoMap`)는 한 번만 만들고 화면 전환마다 재사용(`kakao.maps.event.trigger(..., 'relayout')`로 타일 깨짐 방지).
3. **GPS 실시간 추적 버튼(`toggleMapGps`) 추가.** `navigator.geolocation.watchPosition`으로 내 위치를 계속 구독해서, 이동할 때마다 파란 점 마커(`_kakaoUserMarker`)가 동적으로 따라 이동함. 지도 화면을 벗어나면(`goScreen`에서 감지) 자동으로 GPS 추적을 끄도록 함(배터리 절약, `stopMapGps`).
4. **지도 표시·마커·GPS는 카카오 API 쿼터를 전혀 쓰지 않음을 코드 주석과 화면 안내 문구에 명시.** `bumpKakaoQuota`/`bumpSharedKakaoQuota`는 여전히 `geocodeAddressKakao`(주소→좌표 변환 시점) 안에서만 호출됨 — 지도 표시(`kakao.maps.Map`)와 마커는 별개의 무료 지도 표시 기능이고, GPS는 브라우저 자체 기능이라 카카오 서버와 무관.
5. **(같은 세션, 사용자 피드백 반영) 마을 마커·필지 마커가 겹쳐서 필지 순서 계산이 맞는지 확인이 안 된다는 문제 수정.** ① 지도 화면에 "🏘️ 마을 좌표 / 📍 필지 좌표 / ➰ 필지 순서선" 체크박스 3개 추가 — 레이어별로 켜고 끌 수 있음. ② 필지 순서(`routeMode==='flat'`)가 계산돼 있는 상태로 지도를 처음 열면, 마을 마커는 기본으로 꺼두고 필지만 보이도록 초기값 설정(이후엔 사용자가 체크박스로 자유롭게 전환 — 매번 자동으로 덮어쓰지 않음). ③ 필지 마커를 기존 고정 이미지 핀 대신 **원형 배지 안에 방문 순번 숫자**를 직접 표시하도록 바꿔서(`kakao.maps.CustomOverlay`), 클릭하지 않고도 지도만 보고 순서가 맞는지 바로 확인 가능. ④ 필지 순서가 있을 때 방문 순서대로 파란 선(`kakao.maps.Polyline`)을 이어서 동선 형태를 한눈에 검증할 수 있게 함.

### 이번 세션에 의도적으로 손대지 않은 것
- 엑셀 업로드, 마을 그룹핑, 필지 정렬/동선 계산 로직, 필지 좌표 자동계산 자체의 동작 — 건드리지 않음.
- 지도 위 마커를 드래그해서 좌표를 수정하는 기능, 마커 클러스터링(마커 많을 때 겹침 정리) — 요청받지 않아 미구현.

### 이번 세션 문법/구조 검증
- 인라인 스크립트 전체 `node --check` 통과(구문 오류 없음).
- `<div>` 개수 균형 유지(108/108).
- ⚠️ **`dapi.kakao.com` 접근이 이번 세션 샌드박스에서도 막혀 있어, 실제 브라우저 렌더링(지도가 실제로 뜨는지, 마커가 정확한 위치에 찍히는지, GPS 마커가 실시간으로 움직이는지)은 코드 리뷰로만 확인했고 실측은 못 함.** document.write 수정 자체는 마을 좌표 자동계산 재테스트로 간접 검증됨(SDK 로드가 정상적으로 끝나야 Geocoder 호출도 가능하므로).

### **검증 안 됨 — 다음 세션에서 반드시 확인 필요 (12번째 세션 신규)**
- ⚠️ **[최우선] 지도 보기 화면이 실제로 뜨는지** — 마을/필지 마커가 올바른 위치에 찍히는지, InfoWindow 클릭이 되는지 실브라우저 확인 필요.
- ⚠️ **[신규] 레이어 체크박스(마을/필지/순서선) 토글이 실제로 마커를 켜고 끄는지, 필지 순번 배지 숫자가 실제 방문 순서와 일치하는지, 순서선이 의도한 경로로 이어지는지** — 사용자가 겹침 문제를 보고했던 부분이라 우선순위 높음.
- ⚠️ GPS 버튼으로 내 위치 마커가 실제로 실시간으로 움직이는지(모바일 브라우저에서 이동하며 테스트 권장).
- ⚠️ 필지 좌표 자동계산 자체는 이번 세션에 재테스트 못 함(사용자가 호출량 부담으로 보류) — document.write 수정이 마을 단위에서는 확인됐지만 필지 단위 대량 호출에서도 안정적인지는 미확인.
- ⚠️ 마커가 아주 많을 때(수백~수천 필지) 지도 성능/렌더링 속도, 특히 CustomOverlay(순번 배지) 방식이 마커 이미지보다 무거울 수 있어 대량일 때 체감 속도 확인 필요.

### 11번째 세션 — Firebase 실값 적용 + UI 간소화 + 출발지 키워드 검색

**배경**: 사용자가 `요구사항.txt`와 이전 클로드 답변을 기준으로 `index (9).html`에 미반영된 항목을 모두 적용해달라고 요청함. HANDOFF 10번째 세션에서 설계만 되어 있던 Firebase 공유 사용량 카운터가 실제 HTML에는 없었고, 긴 안내 문구·설정 숨김 쿼터 표시·출발지 Geocoder-only 검색도 개선 대상이었음.

### 실제로 바꾼 것
1. **`FIREBASE_CONFIG`에 사용자 제공 Firebase 프로젝트 값(`juso-dec15`) 채움** — compat SDK 동적 로드(`loadFirebaseSdkScripts`, `ensureFirebaseReady`), `bumpSharedKakaoQuota`, `subscribeSharedKakaoQuota`를 10번째 세션 설계대로 구현. `geocodeAddressKakao()` 성공 시 로컬 카운터와 함께 Firebase `transaction()`으로 공유 카운터도 1 증가.
2. **상단바 `quotaBadge` 추가** — 모든 화면에서 `오늘 남은 조회 N건` 표시. Firebase가 설정돼 있으면 공유 합산 사용량 기준, 아니면 로컬 브라우저 카운터 기준. route 화면의 `kakaoQuotaStatus` 중복 줄은 제거.
3. **설정 모달·업로드 화면 긴 설명 삭제/축短** — `privacy-note` div 삭제, 마을/필지 geo-status 문구 단순화, 설정 모달 카카오 쿼터 장문 안내 삭제(숫자 한 줄만).
4. **`renderKakaoQuota()` 간소화** — `init()`·모든 화면 전환(`goScreen`)·API 호출 후 topbar 배지와 설정 모달을 함께 갱신.
5. **출발지 검색을 `searchStartCandidates()`로 교체** — Geocoder(지번/도로명) + `Places.keywordSearch`(법정동·부분 지명) 병행. 후보가 여러 개면 선택 UI, 1개면 자동 설정. 키워드 검색 성공 시에도 `bumpKakaoQuota` + `bumpSharedKakaoQuota` 호출(검색 1회당 최대 2건 카운트 — 주소 매칭 1 + 키워드 1).

### 이번 세션에 의도적으로 손대지 않은 것
- 엑셀 업로드, 마을 그룹핑, 필지 직접 추가, juso 비활성 코드, 카카오 키 2개 자동 전환, 동선 계산 알고리즘 — 기존 로직 유지.
- 요구사항 CDN의 `type="module"` + Analytics 초기화 블록은 **별도 `<script>`로 넣지 않음** — 단일 HTML(vanilla JS) 구조와 Realtime DB compat SDK 동적 로드 패턴(10번째 세션)이 충돌하지 않도록, 동일 `firebaseConfig` 값만 `FIREBASE_CONFIG` 상수로 사용.

### **검증 안 됨 — 다음 세션에서 반드시 확인 필요 (11번째 세션 신규)**
- ⚠️ Firebase Realtime Database 보안 규칙(8-4절 JSON)이 콘솔에 실제 등록됐는지, 두 브라우저에서 공유 카운터가 실시간으로 같이 오르는지.
- ⚠️ 출발지 `keywordSearch`가 카카오 SDK `libraries=services` 로드 환경에서 실제 후보를 반환하는지.
- ⚠️ `KAKAO_APP_KEYS[1]`이 아직 플레이스홀더 — 두 번째 키 자동 전환은 미설정.

### 10번째 세션 — 파이어베이스 공유 사용량 카운터 실제 구현

**배경**: 9번째 세션에서 "카카오 사용량을 여러 조사원이 함께 보려면 Firebase / Cloudflare Functions+KV / Google Apps Script 중 하나를 골라야 한다"고 비교표만 정리해뒀는데, 사용자가 "클라우드플레어, 파이어베이스 둘다해줘"라고 요청함. **호스팅(Cloudflare Pages)과 사용량 공유 카운터(Firebase)는 서로 다른 목적의 별개 결정**이라고 해석해서 진행함 — 호스팅은 8-1절 계획대로 Cloudflare Pages를 그대로 쓰고(코드 변경 불필요, 정적 파일 배포), 사용량 공유 카운터는 세 후보 중 "서버 코드 없는 단일 HTML 유지"에 가장 잘 맞는 Firebase Realtime Database로 실제 구현함. 같은 목적(숫자 하나 공유)의 백엔드를 Cloudflare KV로 이중 구현하지는 않음 — 필요해지면 다음 세션에 추가 검토 가능하다고 사용자에게 안내함.

### 실제로 바꾼 것
1. **`FIREBASE_CONFIG` 상수 신설** (카카오 키 상수들 바로 아래, `KAKAO_APP_KEYS`와 같은 패턴). 플레이스홀더 상태면 관련 기능 전체가 조용히 꺼지고 기존 동작에 영향 없음 — `isFirebaseConfigured()`가 이 체크를 담당.
2. **Firebase SDK 동적 로드** (`loadFirebaseSdkScripts`, `ensureFirebaseReady`) — 카카오 SDK와 동일한 패턴으로, 정적 `<script>` 태그 대신 `isFirebaseConfigured()`가 참일 때만 `firebase-app-compat.js`/`firebase-database-compat.js`를 동적으로 붙임. 설정 안 한 사용자는 이 스크립트 로드 자체가 안 일어남.
3. **`bumpSharedKakaoQuota(idx)` 신설** — `geocodeAddressKakao()`의 성공 분기에서 기존 `bumpKakaoQuota(idx)`(로컬 카운터) 바로 다음에 호출됨. Firebase Realtime Database의 `kakaoQuotaShared/{키번호}/{날짜}` 경로를 `transaction()`으로 원자적으로 1씩 증가시킴 — 여러 조사원이 동시에 호출해도 카운트가 안 꼬이도록. 논블로킹(await 없이 호출)이라 이 함수가 느리거나 실패해도 지오코딩 자체 흐름은 막지 않음.
4. **`subscribeSharedKakaoQuota()` 신설, `App.init()`에서 호출** — 설정된 각 카카오 키의 오늘 날짜 경로를 실시간 구독해서, 다른 조사원이 호출할 때마다 `_sharedKakaoQuota`가 갱신되고 화면도 자동으로 다시 그려짐(`App.renderKakaoQuota()` 재호출).
5. **`App.renderKakaoQuota()`가 "이 브라우저" 숫자 옆에 "전체 조사원 합산" 숫자를 함께 표시하도록 확장** — Firebase 미설정이면 기존처럼 "이 브라우저 기준" 문구만, 설정돼 있으면 실시간 합산 숫자도 같이 뜸. route 화면 상단(`kakaoQuotaStatus`)과 설정 모달(`kakaoQuotaBox`) 둘 다 동일하게 반영됨.
6. **보안 규칙(Realtime Database Rules) JSON을 코드 주석과 HANDOFF.md 8-4절에 그대로 명시.** "카운트를 1씩 늘리는 쓰기만 허용, 임의 값 덮어쓰기/삭제는 차단"하는 규칙 — 이걸 안 걸면 `apiKey`가 소스에 노출되는 특성상 누구나 카운터를 조작할 수 있어서 필수로 취급함.
7. **설정 모달 안내 문구 갱신** — "카카오 API 사용량 (이 브라우저 기준)"이라는 라벨을 "카카오 API 사용량"으로 바꾸고, `FIREBASE_CONFIG`를 채우면 전체 합산도 보인다는 설명 추가.

### 이번 세션에 의도적으로 손대지 않은 것
- Cloudflare Pages Functions+KV 백엔드는 구현하지 않음(위 배경 참고, 같은 목적 중복 구현 안 함).
- `field_route_planner.html`의 다른 부분(엑셀 업로드, 필지 정렬, juso 관련 비활성 코드 등) — 전혀 손대지 않음.

### 이번 세션 문법/구조 검증
- Firebase 관련 코드 추가 후 JS 전체 문법 오류 없음(`node --check`), HTML `<div>` 개수 균형 유지(101/101, 새 div 추가 안 함).
- 새로 정의한 함수/상수(`FIREBASE_CONFIG`, `isFirebaseConfigured`, `bumpSharedKakaoQuota`, `subscribeSharedKakaoQuota`, `ensureFirebaseReady`, `loadFirebaseSdkScripts`, `_sharedKakaoQuota`)가 코드 전체에서 이름 그대로 정확히 참조되는지 grep으로 확인함.
- `todayKeyKST()`를 정의보다 앞선 위치(`bumpSharedKakaoQuota`, `subscribeSharedKakaoQuota` 내부)에서 참조하지만, `function` 선언은 스코프 안에서 호이스팅되므로 실행 시점엔 문제 없음(코드 리뷰로 확인).

### **검증 안 됨 — 다음 세션에서 반드시 확인 필요 (10번째 세션 신규)**
- ⚠️ **[최우선] `FIREBASE_CONFIG`가 아직 플레이스홀더임.** 실제 Firebase 프로젝트 만들고 값 채우기 + 보안 규칙 등록까지 해야 동작함(8-4절 절차 참고).
- ⚠️ **Firebase SDK를 실제 브라우저에서 로드/호출해본 적이 한 번도 없음** — 이번 세션 샌드박스도 `gstatic.com`/`firebaseio.com` 접근이 막혀 있어 카카오·juso 때와 같은 한계로 실제 네트워크 호출 검증을 못 함. 코드는 Firebase 공식 compat SDK 문서 형식대로 작성했지만, 실제로 스크립트가 로드되는지, `transaction()`이 기대대로 원자적으로 동작하는지는 미확인.
- ⚠️ **동시 다발 호출 시나리오(여러 조사원이 같은 순간에 지오코딩)에서 `transaction()` 재시도 로직이 실제로 카운트 유실 없이 동작하는지 미확인** — Firebase 문서상 `transaction()`은 서버 측 충돌을 감지해 자동 재시도한다고 안내돼 있지만, 이 프로젝트에서 실측한 적은 없음.
- ⚠️ 보안 규칙 JSON이 실제로 "카운트 1씩 증가만 허용"을 의도대로 강제하는지 Firebase 콘솔의 규칙 시뮬레이터로 테스트 안 해봄 — 문법은 Firebase 공식 문서 형식대로 작성했으나 실제 등록 후 검증 필요.

### 9번째 세션 — 비공개 저장소 호스팅 + 카카오 API 발급 절차 정리 (코드 변경 없음, 문서만)

**배경**: 사용자가 이번 세션에서 코드 대신 배포/운영 쪽 질문 세 가지를 함 — ① GitHub 저장소를 비공개로 유지하면서 호스팅할 방법, ② 카카오 API 실제 발급 절차, ③ 카카오 쿼터 사용량을 여러 조사원이 함께 볼 수 있는지(파이어베이스가 꼭 필요한지). **코드(`field_route_planner.html`)는 이번 세션에 전혀 건드리지 않았고**, 조사 결과를 문서화만 함 — 자세한 절차는 새로 만든 "8. 배포 가이드" 절 참고.

### 실제로 한 일
1. **GitHub Pages vs Cloudflare Pages, 비공개 저장소 지원 여부 재확인.** GitHub Pages는 개인 무료 계정에서 비공개 저장소를 못 씀(비공개 저장소 Pages 사용에는 Pro 이상 유료 플랜 필요, 공식 문서로 확인). Cloudflare Pages는 무료 플랜에서도 비공개/공개 저장소 둘 다 공식 지원함(Cloudflare 공식 Git 연동 문서로 확인) → **저장소는 비공개로 두고 Cloudflare Pages로 배포하는 조합**으로 확정. 절차는 8-1절 참고.
2. **카카오맵 API 무료 쿼터 정책이 2026-07-21부로 실제 바뀐 것을 카카오 공식 공지(devtalk.kakao.com)로 재확인.** "추가 기능 신청 + 관리자 심사" 절차가 폐지되고, 앱 관리 페이지에서 즉시 활성화하는 방식으로 간소화됨. 동시에 "개발자 계정 기준 가장 먼저 활성화한 앱 1개에만 무료 쿼터 제공, 두 번째 앱부터는 비즈월렛 연결한 유료 전환"이라는 원칙이 공식 공지로 명문화됨 — 8번째 세션에서 이미 이 방향(계정 2개, 계정마다 앱 1개씩)으로 설계해둔 전략이 정책과 정확히 맞아떨어짐, 전략 변경 없이 그대로 진행. 실제 발급 단계는 8-2절 참고.
3. **카카오 사용량 공유(여러 조사원이 같은 숫자 보기)는 현재의 localStorage 구조상 원천적으로 불가능하다는 점을 확인.** 브라우저 저장소는 그 브라우저 밖을 볼 수 없어서, "몇 명이 같이 쓰는 키를 오늘 총 몇 번 불렀는지"를 여러 사람이 함께 보려면 브라우저 바깥의 공유 저장소가 반드시 필요함 — 이건 파이어베이스를 쓰든 안 쓰든 마찬가지인 전제조건. 파이어베이스가 유일한 해법은 아니라는 게 이번 조사 결론이고, 후보 3가지(Firebase Realtime DB / Cloudflare Pages Functions+KV / Google Apps Script+스프레드시트) 비교표를 8-3절에 정리함. **아직 구현은 안 함** — 사용자가 방식을 고르면 다음 세션에 착수.

### 이번 세션에 의도적으로 손대지 않은 것
- `field_route_planner.html` 코드 전체 — 카카오 키(443번째 줄 `KAKAO_APP_KEYS`)는 여전히 플레이스홀더 상태. 이번 세션은 순수 조사/문서화만 함, 8번째 세션의 "검증 안 됨" 목록도 그대로 유효함.
- 사용량 공유 카운터는 설계 비교만 하고 실제 구현(Firebase 연동 코드, Cloudflare Functions 코드 등)은 시작하지 않음.

### 8번째 세션 — juso.go.kr → 카카오 임시 전환 (GEOCODE_PROVIDER 스위치 도입)

**배경**: 사용자가 juso.go.kr(행정안전부 주소기반산업지원서비스) API를 신청하려 했더니 "이용기관 구분"에서 공공기관/민간기관만 선택 가능하고 개인 신청 항목이 없어, 승인까지 시간이 걸릴 것으로 예상됨. 그동안 서비스를 멈추지 않기 위해 카카오 로컬 API(개인 개발자도 즉시 발급, 무료 일 10만 건)로 임시 전환하되, juso 코드는 지우지 않고 승인 나면 즉시 되살릴 수 있는 구조로 바꿈.

### 실제로 바꾼 것
1. **`GEOCODE_PROVIDER` 상수 신설** (`'kakao'` | `'juso'`, 현재 값 `'kakao'`). 이 값 하나로 지오코딩 제공자가 전체 전환됨 — juso 승인 나면 `'juso'`로 바꾸기만 하면 됨.
2. **juso 관련 코드는 삭제/문자 그대로의 주석처리(`/* */`) 대신, "정의는 되어 있지만 디스패처가 호출하지 않는" 방식으로 비활성화.** 이유: `jusoSearchAddress`/`jusoGetCoord`/`geocodeAddressJuso` 등에 백틱 템플릿 문자열과 중첩 코드가 많아 통짜 블록 주석으로 감싸면 문법 오류 위험이 있고, 다시 켤 때도 주석 해제 실수 가능성이 있음 — 반면 스위치 방식은 사용자가 요청한 "당장 안 쓰지만 지우지 않고, 승인 나면 그대로 되살리기"라는 목적을 그대로 달성하면서 더 안전함. juso 함수 정의부 위에 이 사실과 이유를 주석으로 남겨둠.
3. **`App.geocodeAddress(addrText)` 디스패처 신설** — 마을 단위(`geocodeVillages`), 필지 단위(`geocodeParcels`), 출발지 검색(`searchStartAddress`) 세 호출부 전부 기존 `this.geocodeAddressJuso(...)` 직접 호출에서 `this.geocodeAddress(...)`로 교체. `GEOCODE_PROVIDER` 값에 따라 내부적으로 `geocodeAddressJuso` 또는 신규 `geocodeAddressKakao`로 라우팅.
4. **카카오맵 JS SDK 연동** (`<script src="https://dapi.kakao.com/v2/maps/sdk.js?appkey=...&libraries=services">`). REST API를 브라우저에서 직접 `fetch()`하면 CORS로 막히는 사례가 많아, SDK의 `kakao.maps.services.Geocoder.addressSearch()`(콜백 기반)를 `Promise`로 감싼 `geocoderKakaoRaw()`를 통해 호출하도록 구현. *(이 항목에서 처음 만든 단일 키 구조는 같은 세션 안에서 "카카오 키 2개 자동 전환" 절로 바로 대체됨 — 아래 참고. 최종 코드에는 `KAKAO_APP_KEY` 단일 상수가 아니라 `KAKAO_APP_KEYS` 배열이 들어있음.)*
5. **`geocodeAddressKakao()`는 기존 `parcelCoordCache`(메모리, 세션 한정)를 juso 경로와 그대로 공유.** 캐시 키 포맷(`정규화된 주소 문자열 -> {lat,lng}`)이 동일해서, 나중에 `GEOCODE_PROVIDER`를 juso로 바꿔도 이미 조회한 주소는 재조회하지 않음.
6. **카카오 일일 쿼터(10만 건) 시각화 추가.**
   - `localStorage`에 `frp_kakao_quota = {date:'YYYY-MM-DD', count:N}` 저장, 자정(KST, UTC+9로 명시 계산) 기준 날짜가 바뀌면 자동 리셋(`loadKakaoQuota()`가 매 호출 시 날짜 비교).
   - 성공한 카카오 지오코딩 호출마다 `bumpKakaoQuota()`로 카운트 증가.
   - 설정(⚙) 모달과 동선(route) 화면 상단(`kakaoQuotaStatus`)에 "오늘 사용: N / 100,000 · 남은 추정치" 텍스트로 노출. **의도적으로 "이 브라우저 기준" 추정치라고 명시** — 실제 카카오 쿼터는 앱(API 키) 전체 합산이라, 여러 조사원이 동시에 쓰면 개별 브라우저 카운터로는 전체 잔여량을 정확히 알 수 없음. 이 한계를 설정 모달 안내 문구와 이번 절 모두에 명시해둠.
7. **안내 문구 전반 갱신**: 마을/필지 단위 지오코딩 버튼 옆 설명, 실패 메시지 등에서 "행정안전부 주소 API" → "카카오 지도 API"로 바꾸고, 필지 단위 카드 설명에 "이 앱 전체가 하루 10만 건을 나눠 쓰는 공동 쿼터"라는 문구를 명시적으로 추가함(다음 세션에서도 이 문구를 juso로 되돌릴 때 함께 되돌릴 것).
8. **`geocodeParcels`/`geocodeVillages`의 페이싱(호출 간격) 로직을 provider 조건부로 분기.** juso는 여전히 `JUSO_PACE_MS`(550ms, 짧은 시간 몰아치기 차단 회피용) 적용, 카카오는 별도 speed rate-limit 근거를 확인하지 못했으므로 페이싱 없이(`paceMs=0`) 호출하도록 함 — **이 부분은 검증 안 됨, 아래 참고**.

### 이번 세션에 의도적으로 손대지 않은 것
- juso 관련 함수 본문(`jusoSearchAddress`, `jusoGetCoord`, `geocodeAddressJuso`, `grs80TmToWgs84`, `jsonp`)은 코드 그대로 유지 — 삭제도, 글자 그대로의 주석처리도 하지 않고 "정의만 되고 호출 안 됨" 상태로 둠 (위 2번 항목 이유 참고).
- 필지 직접 추가, 엑셀 업로드/매핑, 마을 좌표 참조표 등 지오코딩과 무관한 기존 기능들 — 전혀 손대지 않음.

### 사용자 질문에 대한 답변 (기록용)
사용자가 "이전 코드는 다 지울까? 주석처리할까?"라고 물어봄 → **삭제는 하지 않기로 함.** juso.go.kr 승인이 막힌 게 아니라 시간이 걸리는 것뿐이고, 승인 나면 juso가 카카오보다 유리(총량 무제한 vs 카카오 공동 쿼터 10만/일)하므로, 승인 대기 중엔 카카오로 임시 운영하고 승인 나면 되돌리는 병행 전략을 그대로 채택함. 실제 구현은 "문자 그대로의 주석처리"보다 안전한 `GEOCODE_PROVIDER` 스위치 방식을 사용함(위 2번 항목 참고) — 사용자가 요청한 목적(당장 비활성화 + 나중에 쉽게 복구)은 동일하게 달성됨.

### 추가 반영 — 카카오 키 2개 자동 전환 (같은 세션 이어서)
**배경**: 카카오 무료 쿼터(키 1개당 일 10만 건)가 조사원 다수에게는 부족할 수 있다는 우려에, 사용자가 "카카오 계정 2개로 키 2개 등록해두고, 하나 소진되면 자동으로 다음 키로 넘어가게" 요청함.

1. **`KAKAO_APP_KEY`(단일) → `KAKAO_APP_KEYS`(배열, 2개) 로 교체.** 서로 다른 카카오 계정으로 만든 앱 2개의 JavaScript 키를 순서대로 넣는 구조.
2. **카카오맵 SDK를 정적 `<script>` 태그(고정 1키) 대신 동적 로드로 변경.** SDK는 키가 `<script src="...appkey=X">`에 박혀 있어 런타임에 키를 바꾸려면 스크립트 자체를 새로 로드해야 함 — `loadKakaoSdkScript()`가 기존 스크립트/​`window.kakao`를 지우고 새 키로 다시 로드하도록 구현(`ensureKakaoSdkLoaded(idx)`가 현재 로드된 인덱스를 추적해 불필요한 재로드는 생략).
3. **⚠️ 중요한 한계 (사용자에게도 알려야 함): 카카오 JS SDK의 Geocoder 콜백은 에러 사유를 세분화해서 주지 않음.** REST API라면 HTTP 429로 "진짜 쿼터초과"를 구분할 수 있는데, SDK는 성공/결과없음/에러 정도로만 알려줘서 **"이 에러가 쿼터초과 때문인지 다른 이유인지 100% 정확히 알 방법이 없음.** 그래서 두 가지 방식을 병행함:
   - **선제 전환**: 현재 키의 이 브라우저 기준 오늘 사용량이 `KAKAO_SWITCH_THRESHOLD`(=10만의 97%, 안전마진)를 넘으면 호출 자체를 시도하지 않고 다음 키로 넘어감.
   - **사후 전환**: 호출 결과가 애매한 에러(`kakao-error`)면 다음 키로 한 번 바꿔서 같은 주소를 재시도함. 단, `no-match`(진짜 주소를 못 찾은 경우)는 쿼터 문제가 아니므로 키를 바꾸지 않고 그대로 실패 처리.
   - 두 키 모두 안전선을 넘었거나 모두 에러면 `{error:'all-keys-exhausted'}`를 반환.
4. **쿼터 저장 키도 `frp_kakao_quota` → `frp_kakao_quota_0`/`frp_kakao_quota_1`로 키별 분리**, `renderKakaoQuota()`가 두 키의 사용량을 각각 표시(현재 사용 중인 키는 "(사용 중)" 표시).
5. **설정 모달 안내 문구 갱신**: "키 2개 등록해두면 자동 전환된다"는 내용과, 위 3번의 한계(정확한 소진 감지는 불가능하고 안전 마진 기반 추정)를 사용자에게도 명시.

### 문서 정리 — 세션 번호 매기기 방식 재정비 (같은 세션 이어서)
사용자가 "2-0-0-0-0 이게 뭐냐, 제대로 정리해봐"라고 지적함 → 세션이 거듭되며 `2-0`, `2-0-0`, `2-0-1`, `2-0-0-0`, `2-0-0-0-0`처럼 임시방편으로 덧붙여온 번호 체계가 알아보기 어려워진 것을 인정하고 전면 재정비함:
- `## 2. 세션별 변경 이력 (최신 세션이 위로)`라는 단일 섹션 아래, `### N번째 세션 — 제목` 형태로 통일(2~8번째 세션 전부).
- 이후 섹션(핵심 기능/설계 이유/검증 현황/할 일/헤더 샘플/파일 위치)도 3~8번으로 순서대로 재번호.
- 문서 내부의 "N절 참고" 같은 교차 참조도 새 번호에 맞게 함께 수정함.

### 7번째 세션 — "필지 직접 추가" 카드 UI 혼동 수정

**배경**: 실제 사용해보니 "추가 항목" 입력칸(열 이름을 정하는 곳)이 카드 맨 위, 소재지 입력보다 먼저 있어서 사용자가 여기에 실제 값("홍길동")을 입력해버리는 혼동이 발생함. "열 이름 정의"와 "그 열의 값 입력"이 이름만 보고는 구분되지 않았음.

### 실제로 바꾼 것
1. **"열 추가" 컨트롤을 본번/부번 아래로 이동.** 기존에는 카드 맨 위(소재지보다도 앞)에 있어서 메인 입력 흐름처럼 보였음 — 이제 소재지 → 본번/부번 → (점선 구분선) → 열 추가 → 열별 값 입력 순서로, "이름 정의"와 "값 입력"이 시각적으로도 분리됨.
2. **라벨을 "추가 항목" → "열 추가"로 변경, 설명 문구 단축.** 기존 설명이 한 줄에 다 들어가서 길었음 — "열 추가 (선택 — 소유자명, 메모 등 같이 기록할 항목의 이름)"으로 축약.
3. **입력칸 placeholder를 "예: 소유자명"(값처럼 보임) → "새 열 이름 입력 후 추가"로 변경**해서, 여기가 실제 값이 아니라 "이름"을 정하는 곳임을 더 분명히 함. 실제 값을 입력하는 하단의 개별 입력칸에는 반대로 "OOO 값 입력" 형식의 placeholder를 붙여서 두 단계가 뚜렷이 구분되도록 함.
4. CSS: 열 추가 섹션을 점선 구분선(`border-top:1px dashed`)으로 감싸 시각적으로 분리함(`.manual-addcol`).

### 이번 세션에 의도적으로 손대지 않은 것
- 데이터 구조(`manualExtraFields`, `manualParcels`, `buildParcelsFromManual` 등)는 그대로 — 순수 UI 배치/문구만 수정.

### 6번째 세션 — 필지 좌표 캐시를 localStorage에서 제거 + 설정 모달 개념 설명 삭제

**배경**: 사용자가 5번째 세션에서 만든 필지 좌표 캐시(`frp_parcel_coord_cache`)가 최대 1~5,000개 규모까지 커질 수 있는데, 이게 애초에 "API 사용량 제한" 때문에 만든 장치인지 물어봄. 마을 단위 캐시(`villageCoordRef`)는 VWorld처럼 무료 쿼터가 있던 시절 사용량을 아끼려고 설계된 것이지만, 필지 단위는 juso.go.kr(무료, 총량 제한 없음, 3번째 세션 항목 참고)을 쓰므로 굳이 브라우저 재시작 후까지 남겨둘 이유가 없다는 게 사용자 판단 — 동의함.

### 실제로 바꾼 것
1. **`parcelCoordCache`를 localStorage 저장/로드에서 완전히 제거, 메모리(탭 세션)에만 유지하도록 변경.**
   - `init()`에서 `localStorage.getItem('frp_parcel_coord_cache')`로 불러오던 코드 삭제 — 이제 새로고침/브라우저 재시작 시 항상 빈 객체(`{}`)로 시작함.
   - `saveParcelCoordCache()` 함수와 그 호출부(`geocodeAddressJuso()` 안) 삭제 — 이제 `this.parcelCoordCache[norm] = {...}`로 메모리에만 쌓이고 별도 저장 단계가 없음.
   - **같은 탭 안에서는 여전히 캐시가 동작함**(같은 파일을 다시 매핑하거나, 필지 목록을 다시 만들어도 이미 조회한 주소는 재조회 안 함) — 탭을 새로고침하거나 닫았다 열면 캐시가 초기화되고 다시 조회함. juso.go.kr이 무료·무제한이라 이 정도 재조회는 비용이 없다고 판단.
   - `villageCoordRef`(마을 좌표 참조표)는 그대로 localStorage에 유지 — 사용자가 "내보내기/불러오기로 직접 관리 가능하니 문제없다"고 확인함. 별도 삭제 버튼은 추가하지 않음(요청 없었음, 필요하면 내보내기 후 브라우저 사이트 데이터 삭제로 정리 가능하다는 점을 사용자에게 안내).
   - `frp_export_cols`(내보내기 항목 선택 기억값)는 논의 대상이 아니었고 용량도 무시할 수준(체크박스 몇 개)이라 그대로 유지.
2. **설정(⚙) 모달에서 "지오코딩 (주소 → 좌표)" 개념 설명 문단 삭제.** "행정안전부 주소기반산업지원서비스... 이미 등록해뒀어요" 문단은 배포 후 최종 사용자(개발자가 아닌 조사원)에게는 조작 가능한 설정이 아니라 순수 배경 설명이라, 사용자 요청대로 지움. 모달에는 이제 프라이버시 안내 문구 + 마을 좌표 참조표(불러오기/내보내기) 섹션만 남음.

### 이번 세션에 의도적으로 손대지 않은 것
- 마을 좌표 참조표(`villageCoordRef`) 관련 로직/문구 — 그대로 유지(사용자가 문제없다고 명시적으로 확인).
- 내보내기 항목 선택 기억값(`frp_export_cols`) — 그대로 유지.
- 필지 직접 추가 기능(5번째 세션분) — 이번 세션과 독립적, 손대지 않음.

### 5번째 세션 — 필지 직접 추가 기능 + 문구 일반화

**배경**: 사용자가 농지 전수조사 외의 다른(더 작은 규모, 애매한 건수의) 조사에도 이 도구를 쓰고 싶어함. 매번 엑셀을 만들어 올리는 게 10개 안팎의 필지에는 번거롭다는 지적.

### 실제로 바꾼 것
1. **업로드 화면 하단에 "필지 직접 추가" 카드 신설.** 엑셀 업로드와 완전히 별개의 경로 — 엑셀 없이 소재지(필수)/본번/부번을 입력해서 필지를 하나씩 추가하고, "추가한 필지로 시작하기" 버튼을 누르면 곧바로 마을 선택 화면(`screen-select`)으로 진입함. 이후 흐름(마을 그룹핑, 좌표계산, 동선 계산, 엑셀 내보내기)은 엑셀 업로드 경로와 완전히 동일한 함수를 그대로 재사용함(`parseAddress`, `jibunLabel`, `jibunSortKey`, `renderVillageList` 등).
   - **엑셀 업로드와 병행/합산은 지원하지 않음.** 둘 중 한 경로만 선택하는 구조 — 사용자가 명시적으로 요청한 범위("업로드 화면 하단에 추가하는 영역")를 그대로 구현했고, 병합 기능은 요청받지 않아 의도적으로 만들지 않음.
2. **"추가 항목" 라벨 입력 영역 신설.** 엑셀 업로드 시 헤더 행에서 소유자명 등 부가 컬럼을 자동 인식하는 것과 대응되는 장치 — 직접 추가 경로에는 헤더 행이 없으므로, 사용자가 부가 항목의 이름을 직접 정하게 함(`App.manualExtraFields`, 예: "소유자명", "메모"). 이 이름이 그대로 (a) 필지 추가 폼의 입력 라벨, (b) 나중에 엑셀로 내보낼 때의 열 제목으로 재사용됨. 항목은 칩(chip) 형태로 표시되고 × 버튼으로 제거 가능.
   - 기존 엑셀 경로의 `EXTRA_KEYS`(소유자명/조사상태/불법의심신고/지목, 농지조사 전용 고정 목록)와는 독립적임 — 직접 추가 경로는 임의의 이름을 쓸 수 있어 농지조사가 아닌 다른 용도의 조사에도 적용 가능.
   - 내보내기 모달(`openExportModal`)이 `this.colMap.extra`의 키 목록을 그대로 사용하는 기존 구조를 그대로 활용함 — 직접 추가 경로에서도 `buildParcelsFromManual()`이 `this.colMap.extra`를 `{항목이름: true}` 형태로 채워 넣어서, 추가 코드 변경 없이 기존 내보내기 항목 선택 모달이 그대로 동작함.
3. **핵심 함수 4개 신규 추가**: `renderManualAddCard()`(폼+칩+추가된 목록 렌더링), `addManualExtraField()`/`removeManualExtraField()`(항목 이름 추가/삭제), `addManualParcel()`/`removeManualParcel()`(필지 추가/삭제), `buildParcelsFromManual()`(기존 `buildParcels()`와 동일한 필지→마을 그룹핑 로직을 배열 기반으로 재구현), `startFromManual()`(진입점, `screen-select`로 이동).
4. **업로드 화면 안내 문구에서 특정 조사 종류를 지목하는 표현 제거.** "농지정보시스템의 기본조사 · 심층조사 · 부적합필지 추출 파일은 물론," 문구를 삭제하고 "소재지(주소) 컬럼이 있는 엑셀이라면 무엇이든 올려보세요."로 시작하도록 일반화함. 그 외 안내 문구(프라이버시 노트, 설정 모달, 매핑 화면 등)는 이미 4번째 세션에서 범용 표현으로 정리돼 있어 추가로 손대지 않음.

### 이번 세션에 의도적으로 손대지 않은 것
- 엑셀 업로드 경로의 파싱/매핑/그룹핑 로직(`autoMapColumns`, `buildParcels` 등) — 그대로 유지, 직접 추가 경로는 별도 함수(`buildParcelsFromManual`)로 병행 구현.
- 법정동코드 입력은 직접 추가 폼에 포함하지 않음 — 소규모 즉석 조사에는 우선순위가 낮다고 판단(사용자 확인 필요, 필요하면 다음 세션에 추가 가능).

### 사용자 질문에 대한 답변 (코드 변경 아님, 기록용)
사용자가 "브라우저를 닫아도 저장되는 데이터가 없는 게 맞냐"고 물어봄 → **정확히는 아님.** `localStorage`에 저장되는 항목 3가지(`frp_village_coord_ref` 마을 좌표 참조표, `frp_parcel_coord_cache` 필지 좌표 캐시, `frp_export_cols` 내보내기 항목 선택값)는 브라우저를 완전히 종료했다가 다시 열어도 그대로 남아있음(같은 브라우저·기기 기준, 사용자가 직접 지우거나 사이트 데이터를 삭제하지 않는 한). 반면 업로드했거나 직접 추가한 필지 목록 자체는 JS 메모리에만 있어서 탭/브라우저를 닫으면 사라짐 — 이 두 가지를 구분해서 사용자에게 안내함.

### 4번째 세션 — VWorld 제거, juso.go.kr로 일원화 + 키 하드코딩

**배경 질의응답 (사용자 확인 필요)**: 사용자가 "공공데이터포털(=행정안전부 juso.go.kr) API가 무료이고 사용량 제한이 없는지, 그렇다면 조사원 개인별 키 입력 대신 개발자가 키 하나를 코드에 박아넣고 배포해도 되는지"를 물어봄. 웹 검색으로 재확인함:
- **무료, 총 호출 건수 제한 없음.** 도로명주소 도움센터 공식 Q&A: "도로명주소 검색 API 호출 시 호출 건 수 제한은 없습니다. DDoS로 보일 정도의 과도한 호출 시 IP 차단될 수 있습니다." → 3번째 세션에 추정했던 "짧은 시간 몰아치기만 제한"이 공식 답변으로 확인됨.
- **승인키는 "URL(또는 IP) 기준"으로 발급됨** (가이드 PDF 20쪽). 즉 조사원 개인이 아니라 **이 앱이 배포될 도메인 하나**에 묶이는 방식 — 그래서 "개인별로 키를 나눠 받을 필요 없이, 이 앱을 특정 고정 도메인(예: GitHub Pages)에 올리고 그 도메인으로 키를 신청해서 코드에 하드코딩하면 조사원 전체가 같은 키를 공유해도 된다"는 사용자의 판단이 맞음.
  - **단, 로컬 파일(`file://`)로 각자 열어서 쓰는 방식이면 안 됨** — 고정 도메인이 없으면 키 등록 시 지정한 URL과 실제 호출 출처가 달라 인증에 실패할 가능성이 큼. 반드시 고정 URL(정적 호스팅)에 배포하고 그 URL로 키를 신청해야 함.
  - 키가 클라이언트 코드(페이지 소스)에 그대로 노출된다는 점은 트레이드오프로 남음 — 도메인 검증 방식이라 다른 도메인에서의 도용은 막히지만, 완전히 숨길 수는 없음. 오남용 걱정되면 언제든 business.juso.go.kr 마이페이지에서 키 재발급/폐기 가능(가이드 PDF 3.2절).

### 실제로 바꾼 것
1. **마을 단위 지오코딩을 VWorld → juso.go.kr로 교체.** 3번째 세션까지는 마을 단위(VWorld)와 필지 단위(juso.go.kr)가 서로 다른 API를 썼는데, 이번에 마을 단위도 필지 단위와 같은 `geocodeAddressJuso()` 파이프라인(검색API→좌표제공API, 캐시 공유)을 재사용하도록 통일함. `geocodeOne`(VWorld 전용 함수)과 VWorld 관련 `getApiKey`/`saveApiKey`를 전부 삭제함.
2. **API 키 입력 UI 전부 제거, 코드 상단에 상수로 하드코딩.** 설정(⚙) 모달에서 VWorld 키 입력칸과 juso 검색/좌표 키 입력칸 2개를 모두 지우고, 대신 "이미 등록돼 있어서 입력 필요 없음" 안내 문구만 남김. 실제 키는 `<script>` 최상단 `JUSO_SEARCH_KEY` / `JUSO_COORD_KEY` 상수에 넣도록 자리만 만들어둠(현재 값은 플레이스홀더 `'YOUR_JUSO_SEARCH_API_KEY'` / `'YOUR_JUSO_COORD_API_KEY'` — **실제 승인키로 반드시 교체해야 동작함**, 아래 "다음 할 일" 참고).
3. **메인 업로드 화면 문구를 범용으로 수정.** "농지정보시스템 엑셀 업로드" → "주소 엑셀 업로드"로 제목을 바꾸고, 설명도 "농지정보시스템 추출본이든, 소재지(주소) 컬럼이 있는 다른 엑셀이든 다 된다"는 취지로 넓힘. 실제 파싱 로직(컬럼 자동탐지 등)은 원래도 특정 서식에 강하게 종속돼 있지 않아서 로직 변경은 없음 — 문구만 바꿈.
4. 그 외 안내 문구(프라이버시 노트, 마을 단위 계산 상태줄, 키 누락 시 얼럿 메시지)도 "VWorld 개인 키 입력" 전제를 지우고 "행정안전부 API, 키 입력 불필요"로 통일함.

### 이번 세션에 의도적으로 손대지 않은 것
- 필지 단위 좌표계산 로직(2단계 호출, 캐시, 중단 버튼 등)은 3번째 세션 그대로 — 함수만 공유하도록 재배치했을 뿐 동작은 동일.
- 마을 좌표 참조표 import/export, GPS 태깅, 법정동코드순 정렬 — 그대로 유지.
- 실제 juso.go.kr 키 발급·호출 자체는 이번에도 실측 못 함 (샌드박스 네트워크 제약, 3번째 세션과 동일한 한계).

### 3번째 세션 — 필지 단위 좌표계산 추가

**사용자가 이번 세션에서 요구사항을 다시 확장함**: 이전 세션은 "마을 단위로만 지오코딩"이 핵심 설계였는데, 이번 세션에서 사용자가 "필지별로 정확한 순서를 계산하고 싶고, 조사 출발지(GPS 또는 텍스트 검색)를 기준으로도 순서를 계산하고 싶다"고 요청함. 즉 마을 단위 방식을 **대체하는 게 아니라 그 위에 추가**하는 형태로 구현함 — 기존 마을 단위 화면/버튼/로직은 전혀 건드리지 않았고, 새 기능은 route 화면에 별도 카드로 추가됨.

### 이번 세션에 확인한 것: data.go.kr에서 찾은 API가 무료인지
사용자가 공공데이터포털에서 발견한 **"행정안전부_실시간 주소별 좌표정보 조회(검색API)"**(data.go.kr 15056663 → 실제로는 business.juso.go.kr로 리다이렉트)를 웹 검색으로 조사함:
- **무료임.** 행정안전부 도로명주소 관련 API 전체가 무료 정부 서비스이고(도로명주소법에 근거한 공익 서비스), 비용 안내나 유료 등급 언급이 어디에도 없음. business.juso.go.kr에서 승인키(confmKey)를 신청하면 즉시 무료로 발급됨.
- **일일 총량 제한은 없어 보임.** 대신 짧은 시간에 몰아서 호출하면 차단되는 방식(제3자 후기 기준 "5초에 10건" 수준, 공식 가이드의 E0007 에러코드 설명과도 일치). 그래서 이번 세션 코드는 "총 건수 제한"이 아니라 "호출 간격"으로 안전장치를 둠(아래 참고).
- **중요한 함정**: 사용자가 찾은 이 API(좌표제공 검색API, `addrCoordApi.do`)는 **주소 문자열을 직접 받지 않음.** 입력값이 행정구역코드(admCd)·도로명코드(rnMgtSn)·건물본번/부번처럼 이미 구조화된 코드임. 주소 문자열 → 이 코드들을 얻으려면 **별도의 "도로명주소 검색API"(`addrLinkApi.do`)를 먼저 호출**해야 하고, 이건 신청도 따로 해야 함(승인키 2개 필요, 같이 못 씀). 그래서 이번 세션은 "검색 API로 주소→코드" 다음 "좌표제공 API로 코드→좌표", 2단계 조합으로 구현함. 두 API 모두 무료.
- 농지(특히 임야·건물 없는 땅)도 이 흐름으로 되는지: 도로명주소 체계는 건물이 없는 땅에도 도로 위치 보간으로 번호(기초번호)를 부여하도록 설계돼 있어서 원칙적으로는 지번 검색도 되어야 하는데, **이번 세션에도 실제 호출 검증은 못 했음** (아래 "검증 안 됨" 참고, 이전 세션의 VWorld와 같은 처지).

### 좌표계 변환이 필요했음
이 API가 돌려주는 X/Y좌표(entX/entY)는 위경도가 아니라 **GRS80 UTM-K(EPSG:5179, "Korea 2000 / Unified CS")** 좌표계임(가이드 PDF의 팝업API 출력 항목에 "좌표정보(GRS80)"라고 명시돼 있음). GPS나 기존 마을 좌표(VWorld, WGS84 위경도)와 같이 쓰려면 변환이 필요해서, 역-횡단메르카토르(Snyder 공식, EPSG:5179 파라미터: lon0=127.5, lat0=38, k0=0.9996, FE=1,000,000, FN=2,000,000) 함수를 직접 구현함(`grs80TmToWgs84`). Node.js로 가이드 PDF의 샘플 좌표(entX=945959.03, entY=1953851.73, "한국지역정보개발원(KLID Tower)")를 변환해보니 위도 37.58 / 경도 126.89가 나왔고, 이건 실제 KLID가 있는 서울 마포구 상암DMC 인근과 일치함 — 변환 공식 자체는 신뢰할 만하다고 판단함(다만 실제 API 응답 JSON의 필드명이 진짜 `entX`/`entY`인지는 검증 못 했음, 가이드 PDF 표기 기준으로만 작성).

### 추가된 기능
1. **필지 단위 좌표 자동계산** ("🧭 필지 좌표 자동계산" 버튼, route 화면의 새 "필지 단위 동선 계산" 카드)
   - 선택된 필지 중 좌표 없는 것만, 필지 소재지 주소 전체를 juso.go.kr에 전송(검색API→좌표제공API 2단계). 마을 단위와 달리 **주소 전체 문자열이 전송된다는 걸 버튼 옆 안내문과 실행 전 confirm 대화상자에 명시**함.
   - 호출 간격은 550ms(정부 API의 "5초 10건" 제한에 여유 있게 맞춤). 필지당 API 2콜 + 대기시간 → 필지당 약 1.1초. 1,000~5,000필지면 20분~90분 정도 걸릴 수 있어서, 시작 전에 예상 소요시간을 confirm 창에 보여주고 "중단하기" 버튼으로 언제든 멈출 수 있게 함.
   - 조회된 좌표는 `frp_parcel_coord_cache`(주소 문자열 → 좌표)에 저장돼서, 같은 파일을 다시 올려도 이미 조회한 필지는 재조회 안 함.
2. **출발지 설정** (GPS 또는 텍스트 검색)
   - "📍 현재 위치를 출발지로": 기존 마을 단위 GPS와는 별개로, 필지 단위 계산 전용 출발지 상태(`App.startPoint`)를 만듦.
   - 텍스트 입력 + "검색" 버튼: 입력한 주소를 juso.go.kr로 지오코딩해서 출발지로 저장. 사무실에서 계획 짤 때나, "여기서부터 출발할 예정"처럼 미리 설정할 때 사용.
3. **필지 기준 순서 계산 / 출발지 기준 순서 계산** — 두 개의 별도 버튼
   - 내부적으로는 같은 최근접 이웃(nearest-neighbor) 알고리즘(`computeFlatOrder`)을 쓰지만, 시작점이 다름: "필지 기준"은 임의의(현재 목록 첫 번째) 좌표 있는 필지에서 시작, "출발지 기준"은 `App.startPoint`(GPS/검색)에서 시작.
   - 계산 결과는 **마을 그룹을 무시한, 필지 하나로 쭉 이어지는 단일 순서 리스트**로 화면이 바뀜(`routeMode='flat'`). "← 마을별 보기로 전환" 링크로 언제든 기존 마을 그룹 화면으로 돌아갈 수 있고, 이 상태는 화면 표시 방식만 바꿀 뿐 데이터(완료 체크 등)는 공유함.
   - 좌표 없는 필지는 순서 계산에서 빠지고 리스트 맨 뒤에 그대로 남음(사용자가 수동으로 위치를 조정할 수 있게).
4. **엑셀 내보내기 대응**: "필지 전체 순서" 모드일 때는 그 순서 그대로, 아닐 때는 기존처럼 마을 그룹 순서대로 내보내도록 분기함.

### 이번 세션에 의도적으로 손대지 않은 것
- 기존 마을 단위 VWorld 지오코딩, GPS 마을 좌표 태깅, 마을 좌표 참조표 import/export, 법정동코드순 정렬 — **전부 그대로 유지**. 사용자가 API 호출량을 줄이고 싶을 때는 여전히 마을 단위 방식을 쓸 수 있고, 정확한 필지별 순서가 필요할 때만 이번에 추가한 기능을 쓰면 됨.

### 2번째 세션 (요약, 배경 참고용)

이전 세션까지는 "서버/외부 API 전송 절대 금지"였는데, 사용자가 이번 세션에서 이 요구사항을 **완화**함:
- 이유: 조사원 1명이 1,000~5,000개 필지를 다루고, 여러 명이 동시에 쓰면 지오코딩 API 사용량이 감당 안 될 수 있다는 우려 + 엑셀에 민감정보(주민번호 등)는 없지만 혹시 모를 유출 방지 차원에서 "필요한 정보만 최소한으로 오가게" 해달라는 요청.
- 그래서 이번 세션은 "API를 아예 안 쓰는 것"이 아니라 **"API 호출 횟수가 필지 수·조사원 수에 비례해서 폭증하지 않는 구조"**로 설계를 바꾸는 데 집중함.

### 핵심 설계 결정: "필지 단위가 아니라 마을 단위로만 지오코딩"
가장 중요한 통찰: 이 도구는 애초에 마을 내부 순서는 지번순(본번→부번)으로 이미 처리하고 있고, 좌표가 실제로 필요한 지점은 **"마을과 마을 사이의 방문 순서"** 뿐임. 즉 필지가 5,000개여도 마을 수는 보통 수십~수백 개 수준이라, 좌표 조회를 마을당 1회로 제한하면 API 호출량이 필지 수와 무관하게 거의 고정됨. 이번 세션에 추가한 기능은 전부 이 원칙 위에 설계함.

### 추가된 기능
1. **지오코딩 API (선택, VWorld 권장) — 설정(⚙)에서 개인 키 입력**
   - "🌐 마을 좌표 자동계산" 버튼: 좌표 없는 마을만, 마을당 1건씩 VWorld Geocoder(`type=parcel` 우선, 실패 시 `type=road`)로 조회. 보내는 값은 "시군구 + 마을명" 문자열뿐 — 소유자명 등 다른 컬럼은 전송하지 않음.
   - API 키는 **조사원 개인이 각자 발급받아 자기 브라우저 localStorage에만 저장**하는 구조로 설계함 (파일에 키를 심어두고 모두가 공유하는 방식이 아님). 이렇게 해야 여러 조사원이 동시에 써도 한 사람이 다른 사람의 무료 할당량을 갉아먹지 않음.
2. **마을 좌표 참조표 (import/export JSON) — 진짜 핵심 절약 장치**
   - GPS로 태깅했든 API로 조회했든, 마을 좌표는 브라우저에 자동 누적 저장됨(`localStorage`). 같은 마을이 나중에 다시 나오면 재조회 안 함.
   - 이 참조표를 JSON으로 내보내서 동료에게 공유 가능 → 한 사람(또는 담당자 1명)이 해당 시군구의 마을 좌표를 한 번만 만들어두면, 나머지 조사원들은 API를 아예 안 써도 됨. 시군구 하나에 마을이 수백 개 수준이라면, 팀 전체가 통틀어 API를 수백 번만 쓰면 끝나는 구조.
3. **API 없는 대안 — 법정동코드순 정렬**
   - "🔢 법정동코드순 정렬" 버튼: 엑셀에 `(2)법정동코드` 컬럼이 있으면(대부분의 추출본에 있음), 좌표 없이 그 코드 순서로 마을을 정렬. 법정동코드가 지리적 인접성을 완벽히 반영하진 않지만, 최소한 결정론적이고 API 호출이 전혀 없는 옵션으로 추가함.
   - 컬럼 확인 화면에 "법정동코드" 매핑 행 추가함 (기존 소재지/본번/부번과 동일한 방식).
4. **마을 순서 추천 로직 개선**: 기존에는 반드시 라이브 GPS 위치가 있어야 "마을 순서 추천"이 활성화됐는데, 이제는 **좌표 있는 마을이 2개 이상이면** (GPS 없이 사무실에서 미리 계획할 때도) 동작하도록 바꿈. GPS가 없으면 현재 순서상 첫 번째로 좌표가 있는 마을을 출발점으로 삼아 최근접 이웃 방식으로 나머지를 정렬.
5. **엑셀 재추출 시 항목 선택 (보안/최소정보 원칙)**
   - "엑셀로 내보내기" 클릭 시 바로 저장되지 않고, 먼저 내보낼 추가 항목(소유자명/조사상태/불법의심신고/지목)을 체크박스로 고르는 모달이 뜸.
   - 순서·마을·시군구·소재지·본번·부번·완료여부는 항상 포함(현장 식별에 필요한 최소 정보), 그 외 항목은 기본적으로 전부 **선택 해제 상태**로 시작 — 사용자가 명시적으로 체크해야 포함됨.
6. **다중 시트 대응**: 엑셀에 시트가 2개 이상이면 업로드 직후 시트 선택 화면이 뜸. "소재지" 키워드가 있는 시트를 자동으로 추천하되 사용자가 바꿀 수 있음. (이전에는 항상 첫 번째 시트만 읽어서, 표지/안내 시트가 첫 시트면 깨지는 문제가 있었음 — 이번에 해결)
7. **지번 정렬 버그 수정**: 본번에 "산44"처럼 숫자가 아닌 값(임야 표기)이 오면 기존 로직은 전부 정렬 맨 끝의 같은 그룹(사실상 무작위 순서)으로 몰아넣었음. 이번에 "산" 표기를 인식해서, 일반 필지는 그대로 지번순으로 정렬하고 산 표기 필지는 그 뒤에 별도로 지번순 정렬되도록 고침.

### 조사한 내용 (이번 세션, 웹 검색 기반 — 2026년 8월 기준, 바뀔 수 있음)
- **VWorld(브이월드) Geocoder API**: 일일 무료 한도 약 3만 건, 지번(`type=parcel`)·도로명(`type=road`) 주소 모두 지원 — 건물이 없는 농지(임야 포함)도 연속지적도 기반이라 지오코딩 가능. 단, 공식 안내상 "실시간 사용" 전제이고 별도 DB에 영구 저장하는 용도로는 쓰지 말라는 안내가 있음 (참고: vworld.kr Geocoder API 레퍼런스).
- **카카오 로컬 API**: 2026년 7월 21일부로 무료 쿼터 운영 방식이 바뀜 — 개발자 계정 기준 "가장 먼저 활성화한 앱 1개"에만 무료 쿼터가 제공되고, 두 번째 앱부터는 비즈월렛 연동한 유료 과금 구조로 전환됨. 또한 카카오 REST API(`dapi.kakao.com`)는 브라우저에서 직접 `fetch()`로 호출 시 CORS 에러가 흔히 보고됨 — 서버 없이 브라우저에서만 도는 이 도구 구조에는 카카오 지도 JavaScript SDK의 `kakao.maps.services.Geocoder`(스크립트 태그로 로드하는 방식)가 더 적합해 보이지만, 이번 세션에는 VWorld만 실제로 연동함. 카카오 연동은 다음 세션 검토 대상으로 남겨둠.
- **행정안전부 주소기반산업지원서비스(business.juso.go.kr)**: "위치정보요약DB"라는, 주소와 좌표가 이미 매칭된 다운로드용 데이터셋을 제공함. 이걸 미리 받아서 마을 참조표를 대량으로 미리 채워두는 방법도 있음 — 다만 이번 세션 샌드박스 환경에서는 이 도메인에 네트워크 접근이 막혀 있어 실제로 받아보지 못했음 (아래 "검증 안 됨" 참고). 다음 세션에 사용자가 직접 받아서 넘겨주면 참조표 임포트 기능으로 바로 활용 가능.

## 3. 핵심 기능 (전체, 갱신본)

1. 엑셀 업로드 → (다중 시트면 시트 선택) → 헤더 행 자동 탐지 → 소재지/본번/부번/법정동코드 컬럼 자동 매핑 (틀리면 수동 수정 가능)
2. 소재지 주소 문자열을 시도/시군구/읍면동/리로 파싱해서 마을 단위로 자동 그룹핑
3. 시군구 필터 + 마을명 검색으로 좁혀서, 오늘 조사할 마을만 체크
4. 마을 순서 / 마을 내 필지 순서를 ▲▼ 버튼으로 수동 조정 (드래그 아님 — 모바일 터치 신뢰성 때문에 의도적으로 버튼 선택)
5. 필지 기본 정렬은 본번→부번 오름차순 (임야 "산" 표기는 일반 필지 뒤에 별도로 오름차순 정렬)
6. 마을 좌표 확보 경로 3가지, 어느 것을 써도 이후 로직은 동일하게 동작:
   - GPS: 브라우저 Geolocation으로 "내 위치 확인" + 마을별 "📍 이 마을 좌표로 저장"(최초 1회 현장에서 태깅)
   - API: 설정에서 개인 VWorld 키 입력 후 "🌐 마을 좌표 자동계산" (마을당 1건)
   - 참조표 임포트: 동료가 내보낸 마을 좌표 JSON을 불러오기 (API 호출 없음)
   - 좌표 있는 마을이 2개 이상이면 "🧭 마을 순서 추천"으로 최근접 이웃 방식 방문 순서 계산 (GPS 없어도 동작)
7. API/GPS 둘 다 없을 때의 대안: "🔢 법정동코드순 정렬" (좌표 없이, 엑셀에 있는 법정동코드로 결정론적 정렬)
8. 방문 완료 체크 → 언제든 엑셀로 재추출. 재추출 시 어떤 추가 항목(소유자명 등)을 포함할지 직접 선택 가능 — 기본은 최소 정보만 포함.
9. **[이번 세션 신규] 필지 단위 동선 계산** (route 화면 하단 별도 카드, 마을 단위 기능과 독립적으로 공존)
   - "🧭 필지 좌표 자동계산": 설정에서 행정안전부 juso.go.kr 키 2개(검색API, 좌표제공API) 입력 후, 선택된 필지 전체(마을 그룹 무시)의 소재지 주소를 개별 지오코딩. 필지당 약 1.1초, 진행 중 "중단하기"로 취소 가능, 결과는 브라우저에 캐시되어 같은 파일 재업로드 시 재조회 안 함.
   - 출발지 설정: "📍 현재 위치를 출발지로"(GPS) 또는 주소 텍스트 입력 후 "검색"(juso.go.kr로 지오코딩).
   - "📐 필지 기준 순서 계산": 임의의 필지에서 시작하는 최근접 이웃 순서.
   - "🚩 출발지 기준 순서 계산": 위에서 설정한 출발지에서 시작하는 최근접 이웃 순서.
   - 두 버튼 모두 결과는 마을 그룹을 무시한 단일 순서 리스트로 화면이 바뀜(`routeMode:'flat'`). "← 마을별 보기로 전환"으로 언제든 복귀 가능. 완료 체크·엑셀 내보내기 모두 이 모드를 인식함.

## 4. 왜 이렇게 설계했는가 (중요 결정 배경)

- **필지가 아니라 마을 단위로만 지오코딩하는 이유**: 사용자의 핵심 우려("조사원 1명당 1,000~5,000건, 여러 명 쓰면 API 사용량 폭증")에 대한 답. 마을 내부 순서는 지번순으로 이미 합리적인 근사치가 있으므로, 좌표가 진짜 필요한 곳은 마을 간 이동 순서뿐. 이 통찰 하나로 API 호출량을 "필지 수 비례"에서 "마을 수(주로 필지 수의 1/10~1/50 수준) 비례"로 줄임.
- **API 키를 파일에 심지 않고 개인별로 입력받는 이유**: 하나의 키를 여러 조사원이 공유하면 한 사람의 대량 조회가 다른 사람의 무료 할당량을 다 써버릴 수 있음. 개인별 키 + 로컬 저장 구조로 이 문제를 원천 차단.
- **마을 좌표 참조표를 굳이 만든 이유**: 같은 시군구를 여러 조사원이 나눠서 조사하면 마을 이름은 겹치는 경우가 많음. 담당자 한 명이 한 번만 좌표를 만들어 공유하면, 팀 전체의 API 사용량이 "조사원 수 × 필지 수"가 아니라 "지역의 고유 마을 수" 딱 한 번으로 끝남.
- **좌표 기반 실거리 동선 최적화가 여전히 완벽하지 않은 이유**: 마을 좌표는 마을 대표 좌표(centroid) 하나일 뿐, 마을 내부 필지들의 정확한 위치가 아님. 마을 내부는 여전히 지번순 + 수동 조정에 의존함. 필지 단위 정밀 좌표가 필요하면 API 사용량이 다시 필지 수에 비례해서 늘어나므로, 이번 세션에서는 의도적으로 구현하지 않음 (다음 세션 논의 대상, 4절 참고).
- **VWorld를 우선 선택한 이유**: 지번(parcel) 주소 지오코딩을 공식 지원해서 건물이 없는 농지·임야도 좌표 변환이 가능하고, 브라우저에서 직접 `fetch()` 호출이 잘 되는 사례가 많이 보고됨. 카카오 REST API는 CORS 문제가 흔히 보고되어 이 "서버 없는 단일 HTML" 구조에는 상대적으로 덜 맞음 (3번째 세션 항목 참고, 다만 8번째 세션에서 결국 카카오로 전환하며 JS SDK 방식으로 이 CORS 문제를 우회함).
- **드래그앤드롭 대신 ▲▼ 버튼**: 야외 터치스크린(장갑 낀 손 등) 환경 고려. (유지)
- **React 대신 순수 HTML/JS**: 1000~5000행 규모 리스트를 다루므로 리렌더 비용을 직접 통제하려고 vanilla JS 선택. (유지)
- **[이번 세션] 필지 단위 지오코딩에 juso.go.kr을 쓴 이유**: 무료이고, 정부 공식 도로명주소 체계라 커버리지가 넓을 것으로 기대되고, 일일 총량 제한이 없어(짧은 시간 몰아치기만 제한) 수천 건 규모에도 이론상 대응 가능하기 때문. 다만 "주소 문자열 → 코드 → 좌표" 2단계 구조라 필지당 API 콜이 2배(VWorld는 1콜)라는 트레이드오프가 있음.
- **[이번 세션] fetch/CORS 대신 JSONP를 쓴 이유**: 가이드 PDF의 "웹 페이지에서 직접 API 이용방법" 절이 명시적으로 JSONP 방식(`addrLinkApiJsonp.do`/`addrCoordApiJsonp.do`)을 공식 지원 경로로 안내하고 있어서, CORS 지원 여부가 불확실한 일반 `fetch()`보다 이 방식이 더 안전한 선택이라고 판단함. (VWorld는 반대로 CORS `fetch()`를 썼는데, 이건 검증 못 한 채로 남아있음 — 5절 참고)
- **[이번 세션] 마을 단위 방식을 지우지 않고 필지 단위를 "추가"만 한 이유**: 사용자가 이번에 요청한 건 "정밀한 필지별 순서"이지 "마을 단위 방식이 틀렸다"는 게 아님. 여러 명이 나눠 쓸 때 API 사용량을 아끼고 싶으면 여전히 마을 단위(호출 적음)를, 필지 하나하나의 정확한 동선이 필요하면 필지 단위(호출 많음, 시간 걸림)를 상황에 맞게 고를 수 있게 두 옵션을 공존시킴.

## 5. 검증됨 vs 검증 안 됨 (제일 중요)

### 검증됨 (Node.js로 로직만 시뮬레이션 — 이번 세션 포함, 실제 브라우저 테스트는 여전히 불가능한 환경)
- 주소 파싱 함수(`parseAddress`)가 세종특별자치시(시군구 없음), 서울 동(리 없음), 일반 도/시/읍/면/리 패턴에서 올바르게 시도/시군구/읍면동/리를 분리 → 통과 (기존 검증 유지)
- 헤더 자동 탐지(`detectHeader`), 컬럼 자동 매핑(`autoMapColumns`, 법정동코드 매핑 추가분 포함) → 통과
- 필지→마을 그룹핑과 마을 내 지번 오름차순 정렬 → 통과
- **[이번 세션 신규]** 지번 정렬의 "산"(임야) 표기 처리 — 일반 필지 오름차순 다음에 산 표기 필지가 오름차순으로 이어지는지 → 통과
- Haversine 거리 계산(서울-부산 약 325km) → 통과 (기존 검증 유지)
- **[이번 세션 신규]** 법정동코드순 정렬 비교 함수 — 숫자로 파싱 가능하면 숫자 비교, 코드 없는 마을은 뒤로 보내는지 → 통과
- **[이번 세션 신규]** 엑셀 재추출 시 "선택 안 한 항목은 결과에서 빠지는지" 필터링 로직 → 통과
- 전체 JS 문법 오류 없음 (`node --check`, 이번 세션 수정분 포함)
- HTML div 태그 개수 균형 확인, 이번 세션에 새로 참조한 모든 DOM id가 실제로 HTML에 정확히 1번씩 존재하는지 확인
- **[3번째 세션 신규]** `grs80TmToWgs84` (EPSG:5179 GRS80 UTM-K → WGS84) 변환 함수를, 가이드 PDF에 실린 실제 샘플 좌표(entX=945959.0381341814, entY=1953851.7348996028, "한국지역정보개발원(KLID Tower)")로 Node.js에서 역산 → 위도 37.5825 / 경도 126.8879가 나왔고, 이는 실제 KLID가 위치한 서울 마포구 상암DMC 인근 좌표와 부합함 → **변환 공식 자체의 정확성은 이 한 지점 기준으로 검증됨.** (다만 실제 API 응답이 이 필드명/좌표계를 그대로 쓰는지는 별개 — 아래 검증 안 됨 참고)
- **[3번째 세션 신규]** JS 문법 오류 없음(`node --check`, 필지 단위 기능 추가분 포함), HTML id/div 균형 재확인

### 검증됨 (4번째 세션 추가분)
- VWorld 관련 코드/입력칸을 지운 뒤 남은 JS 전체 문법 오류 없음(`node --check`), HTML `<div>` 개수 균형 확인, 삭제한 함수/엘리먼트 id(`vworldKeyInput`, `jusoSearchKeyInput`, `jusoCoordKeyInput`, `saveApiKey`, `saveJusoKeys`, `getApiKey`, `geocodeOne`)를 코드 전체에서 검색해 참조가 하나도 안 남았는지 확인함.

### 검증됨 (5번째 세션 추가분)
- 필지 직접 추가 기능 추가 뒤 JS 전체 문법 오류 없음(`node --check`), HTML `<div>` 개수 균형 확인(99/99), 새로 참조한 모든 DOM id(`manualAddCard`, `manualExtraLabelInput`, `manualExtraFieldChips`, `manualParcelForm`, `manualAddrInput`, `manualBonInput`, `manualBuInput`, `manualExtraInput_{i}`(동적 생성), `manualParcelListWrap`, `manualStartBtn`)가 실제 HTML/동적 렌더링에 정확히 존재하는지 확인함.
- `buildParcelsFromManual()`이 기존 `buildParcels()`와 동일한 필드 구조(`id,addr,bon,bu,jibun,sortKey,villageKey,villageName,sigunguKey,extra,coord,done`)를 만드는지 코드 리뷰로 대조 확인 — 다운스트림(마을 그룹핑, 지번 정렬, 좌표 계산, 동선 계산, 엑셀 내보내기)이 두 경로 모두에서 동일하게 동작해야 하므로 특히 신경 씀.

### 검증됨 (6번째 세션 추가분)
- `frp_parcel_coord_cache` 관련 코드(로드/저장) 삭제 뒤 JS 전체 문법 오류 없음(`node --check`), HTML `<div>` 개수 균형 확인(99/99), 삭제한 함수(`saveParcelCoordCache`)를 코드 전체에서 검색해 참조가 하나도 안 남았는지 확인함.
- `parcelCoordCache`를 참조하는 다른 세 곳(`buildParcels`, `buildParcelsFromManual`, `geocodeAddressJuso`)이 여전히 메모리 객체 기준으로 정상 동작하는 구조인지 코드 리뷰로 확인 — 로드 안 해도 `{}` 기본값으로 시작하므로 문제 없음.

### 검증됨 (8번째 세션 추가분)
- `GEOCODE_PROVIDER` 스위치 도입 뒤 JS 전체 문법 오류 없음(`node --check`), HTML `<div>` 개수 균형 확인(101/101).
- 새로 참조한 DOM id(`kakaoQuotaStatus`, `kakaoQuotaBox`)가 HTML에 실제로 존재하는지 확인함.
- `geocodeAddressKakao()`가 기존 `parcelCoordCache`와 동일한 키/값 형식(`정규화 주소문자열 -> {lat,lng}`)을 쓰는지 코드 리뷰로 대조 확인 — `GEOCODE_PROVIDER`를 나중에 `'juso'`로 바꿔도 캐시가 깨지지 않아야 하므로 특히 신경 씀.
- `geocodeVillages`/`geocodeParcels`/`searchStartAddress` 세 호출부 모두 `this.geocodeAddressJuso` 직접 참조가 남아있지 않고 `this.geocodeAddress` 디스패처를 통하는지 grep으로 확인함.

### **검증 안 됨 — 다음 세션에서 반드시 확인 필요**
- ⚠️ **[11번째 세션 신규]** Firebase 공유 카운터 + topbar `quotaBadge` + 출발지 키워드 검색 — 코드 반영 완료, 실제 브라우저/배포 환경에서 미검증.
- ⚠️ **[10번째 세션]** `FIREBASE_CONFIG` 값은 11번째 세션에서 채워짐 — Realtime Database 보안 규칙 등록 및 다중 브라우저 실측은 여전히 필요.
- ⚠️ **[8번째 세션 신규, 최우선]** `KAKAO_APP_KEYS` 배열 2번째 칸이 아직 플레이스홀더(`'YOUR_KAKAO_JS_APP_KEY_2'`)임 — 카카오 디벨로퍼스에서 (서로 다른 계정으로) 앱 2개 생성, 각각 배포 도메인을 "플랫폼 > Web"에 등록, 발급된 **JavaScript 키**(REST 키 아님) 2개로 교체해야 지오코딩이 실제로 동작함. 이번 세션은 코드 구조만 작성했고, 실제 브라우저에서 카카오 SDK를 호출해본 적은 없음(샌드박스 네트워크 제약, `dapi.kakao.com` 접근 불가). 특히 **동적 스크립트 교체(`loadKakaoSdkScript`)로 키 전환이 실제 브라우저에서 매끄럽게 되는지, 전환 중 진행 중이던 지오코딩 요청이 꼬이지 않는지 실측 필요.**
- ⚠️ **[8번째 세션 신규]** 키 전환 로직이 "선제 전환(안전 마진 기준) + 사후 전환(애매한 에러 시 재시도)" 방식인데, **카카오 SDK가 진짜 쿼터초과와 다른 에러를 구분해주지 않아 정확도가 100%가 아님.** 실제로 한 키의 쿼터를 소진시켜보고(테스트 계정으로) 이 로직이 의도대로 다음 키로 넘어가는지, 아니면 오탐(다른 에러인데 키를 낭비하며 전환)이 잦은지 실측 필요.
- ⚠️ **[8번째 세션 신규]** `kakao.maps.services.Geocoder.addressSearch()`가 juso.go.kr과 달리 **도로명주소 위주로 설계**되어 있어, 건물이 없는 순수 지번(농지·임야) 주소 인식률이 juso나 VWorld보다 낮을 가능성이 있음 — 실측 필요. 낮으면 지번을 도로명으로 먼저 변환하거나, 시군구+마을명까지만 잘라서 재시도하는 폴백 로직이 필요할 수 있음(아직 구현 안 함).
- ⚠️ **[8번째 세션 신규]** 카카오 API의 "짧은 시간 몰아치기" 속도 제한(초당 호출 수 제한) 여부와 수치를 문서로 확인하지 못해, 이번 세션은 `paceMs=0`(페이싱 없음)으로 구현함 — 실제로 429 에러가 나는지, 나면 얼마나 빨리 나는지 실측 후 필요시 페이싱 추가해야 함.
- ⚠️ **[8번째 세션 신규]** `frp_kakao_quota`(localStorage 카운터)는 "이 브라우저가 오늘 몇 번 호출했는지"만 셀 뿐, **카카오 서버가 실제로 얼마나 소진됐는지와는 무관한 추정치**임 — 여러 조사원이 각자 다른 브라우저로 같은 앱 키를 쓰면 이 숫자들의 합이 진짜 쿼터 소진량이지만, 개별 브라우저는 그 합을 알 방법이 없음. UI 문구로는 명시했으나, 실제 다인원 사용 시나리오에서 사용자 혼란이 없을지는 실사용 후 확인 필요.
- ⚠️ **[5번째 세션 신규]** 필지 직접 추가 카드의 실제 브라우저 동작(모바일 터치 키보드, 항목 추가/삭제 시 스크롤 위치, 필지 10개 이상 추가했을 때 리스트 UX)을 실제로 실행해본 적 없음 — 기존 항목들과 마찬가지로 이 세션도 코드 리뷰 + Node 시뮬레이션 수준 검증만 가능한 샌드박스 환경.
- ⚠️ **[4번째 세션 신규, 최우선]** `JUSO_SEARCH_KEY`/`JUSO_COORD_KEY`가 아직 플레이스홀더 문자열임 — 실제 키로 교체하고 고정 도메인에서 실제 호출까지 해봐야 이번 세션 변경분 전체(마을 단위 지오코딩 포함)가 검증됨. 이번 세션은 코드 구조 변경만 했을 뿐, 실제 브라우저에서 juso.go.kr을 호출해본 적은 3번째 세션과 마찬가지로 없음.
- ⚠️ **[4번째 세션 신규]** "승인키가 URL(도메인) 단위로 검증된다"는 것을 가이드 PDF 문구로만 확인했고, 실제로 어떤 HTTP 헤더(Referer? Origin?)를 보는지, `file://`로 열었을 때 정확히 어떤 에러가 나는지는 실측 못 함.
- ⚠️ **실제 농지정보시스템 엑셀 파일로 테스트한 적이 여전히 한 번도 없음.** (기존 항목, 미해결 — 최우선 순위 그대로)
- ⚠️ **[이번 세션 신규 발견]** `parseAddress`가 "전라북도" 같은 완전한 시도명은 인식하지만 **"전북" 같은 축약형은 인식하지 못하고 `rest`로 빠짐** (Node 시뮬레이션에서 실제로 재현 확인함). 농지정보시스템 실제 추출본이 축약형을 쓰는지 전체 시도명을 쓰는지 확인 안 됨 — 축약형이면 `sigunguKey`에 시도가 빠진 채로 시군구만 남아, 동명 시군구가 다른 도에 있을 경우(예: 고성군이 강원·경남 둘 다 있음) 마을을 잘못 합칠 위험이 있음.
- ⚠️ **VWorld Geocoder 실제 연동은 이번 세션에도 검증 못 함.** 이 샌드박스의 네트워크 정책상 `vworld.kr` 도메인 자체에 접근이 막혀 있어서, 코드는 공식 문서 형식대로 작성했지만 실제 키로 호출해본 적이 없음. 특히:
  - 실제 브라우저에서 CORS가 통과하는지 (문서/블로그 사례상 되는 경우가 많다고는 확인했으나 직접 확인은 못 함)
  - 응답 JSON 구조(`response.result.point.x/y`)가 실제로 정확한지
  - 무료 키 발급 절차, 도메인 등록 필요 여부(키가 특정 도메인에 묶이는지) 확인 안 됨 — 만약 도메인 제한이 있으면 로컬 파일(`file://`)로 열었을 때 인증이 안 될 수 있음
- ⚠️ **카카오 API는 이번 세션에 아예 연동하지 않음** (CORS 우려로 VWorld만 구현). 카카오 지도 JS SDK 방식(`kakao.maps.services.Geocoder`)으로 대안 구현할지는 다음 세션 논의 대상.
- ⚠️ **행정안전부 위치정보요약DB(business.juso.go.kr) 다운로드는 시도조차 못 함** — 샌드박스 네트워크 정책상 해당 도메인 접근 불가. 이 데이터를 참조표에 대량으로 미리 채워 넣는 흐름은 사용자가 직접 받아서 시험해봐야 함.
- ⚠️ 실제 브라우저(특히 모바일 Safari/Chrome)에서 파일 업로드 → 시트 선택 → 렌더링 → GPS 권한 요청 → API 키 저장(localStorage) → 엑셀 재추출까지 실제로 동작하는지 **한 번도 실행해보지 않음** (기존 항목, 미해결)
- ⚠️ 새 설정 모달/내보내기 모달의 실제 모바일 터치 UX(모달 스크롤, 키보드 겹침 등) 미확인
- ⚠️ 본번/부번 케이스는 이번에 "산" 접두어만 다뤘음 — 공백, 텍스트 혼재 등 다른 비정상 패턴은 여전히 미확인
- ⚠️ 1000~5000행 규모에서 실제 브라우저 렌더링 성능 미확인 (기존 항목, 미해결)
- ⚠️ 소재지 문자열에 "외 3필지", 도로명 병기, 이중 공백 등 실제 행정 데이터 특유의 노이즈 처리 여부 미확인 (기존 항목, 미해결)

## 6. 다음 세션에서 할 일 (우선순위 순)

0. **[8번째 세션 신규, 최우선 — 9번째 세션에서 절차 문서화 완료, 실행은 아직 안 됨] `KAKAO_APP_KEYS` 플레이스홀더를 실제 JavaScript 키 2개로 교체.** 구체적 절차는 8절 "배포 가이드" 참고(비공개 저장소+Cloudflare Pages 배포 → 고정 도메인 확보 → 카카오 계정 2개로 앱 발급). 마지막 단계: 배포 후 마을/필지 지오코딩, 쿼터 카운터가 실제로 오르는지 테스트.
0-2. **[10~11번째 세션] `FIREBASE_CONFIG` 값 채움 완료 — Realtime Database 보안 규칙 붙여넣기 + 다중 브라우저 실측만 남음.** 8-4절 JSON을 Firebase 콘솔에 등록한 뒤, 브라우저 두 개로 지오코딩 호출 시 topbar `quotaBadge` 숫자가 양쪽에서 같이 줄어드는지 확인.
0-1. **[8번째 세션 신규] juso.go.kr 승인 신청은 별도로 계속 진행.** 승인 나면 `GEOCODE_PROVIDER`를 `'kakao'`에서 `'juso'`로 바꾸기만 하면 됨(코드 재작성 불필요) — `JUSO_SEARCH_KEY`/`JUSO_COORD_KEY` 플레이스홀더 교체는 그때 진행.
1. **사용자에게 실제 엑셀 파일(또는 개인정보 제거한 더미본) 받아서 실제로 업로드 테스트** — 여전히 최우선. 특히 이번에 발견한 "시도 축약형(전북 등)" 인식 여부부터 확인.
2. **[8번째 세션 신규] 카카오 지오코딩의 지번(농지·임야) 주소 인식률 실측** — juso/VWorld 대비 낮을 가능성 있음(위 5절 참고). 낮으면 폴백 로직(시군구+마을명만으로 재시도 등) 추가 검토.
3. 실기기(모바일)에서 GPS 버튼, ▲▼ 버튼, 새 설정/내보내기 모달 터치 반응 직접 확인 요청
4. 시도 축약형("전북", "경기" 등) 대응 — `classifyToken`/`LEVEL1`에 축약형 목록 추가 검토 (실제 데이터 확인 후 착수)
5. 본번/부번의 "산" 외 비숫자 케이스(공백, 텍스트 혼재) 처리 로직 보강
6. 마을 경계 필지("이 필지는 옆 마을이 더 가까움") 처리 방식 논의 — 여전히 보류 중인 항목
7. 대용량(수천 행) 실제 렌더링 성능 확인, 필요시 가상 스크롤 등 도입 검토
8. (선택) 행정안전부 위치정보요약DB를 사용자가 직접 받아서 넘겨주면, 이를 파싱해서 마을 좌표 참조표 포맷으로 일괄 변환하는 가져오기 기능 추가 검토

## 7. 참고용 원본 헤더 샘플 (사용자가 대화 중 제공)

### 심층조사 추출 헤더 — 변형 1
```
순번	(1)소재지	(2)법정동코드	(3)대장구분코드	(4)본번	(5)부번	공부지목	공부면적	소유자명	소유인수	시설물	시설물	시설물	시설물	조사상태	불법의심신고	심층조사결과
```

### 심층조사 추출 헤더 — 변형 2 (시설물 4종 세분화)
```
순번	(1)소재지	(2)법정동코드	(3)대장구분코드	(4)본번	(5)부번	공부지목	공부면적	소유자명	소유인수	농축산물생산시설	농지개량시설	전용행위(일반시설등)	노지상태인농지	조사상태	불법의심신고	심층조사결과
```

### 기본조사 추출 헤더 (3단 병합 헤더, 요약)
- 1행: (1)소재지 / 소재지 / (4)본번 / (5)부번 / 소유자명 / 소유인수 / 주말·체험영농여부 / 심층조사대상여부(9대) / 심층조사대상주체(9대) / 건축물대장등재여부 / 소유관계조사(8열) / 실경작자조사(4열) / 이용현황조사(6열) / 기본조사결과 / 심층조사대상추가여부 / 조사상태 / 불법의심신고 / AI탐지
- 소유관계조사 하위: ①비농업인 상속 농지 ②이농한 자의 소유 농지 ③농업회사법인 소유 농지 ④일반법인·단체 소유 농지 (각 추정/결과 2열씩)
- 실경작자조사 하위: ⑤자경 농지 ⑥임대차(사용대차) 농지 (각 추정/결과 2열씩)
- 이용현황조사 하위: ⑦주말·체험영농 및 휴경 등 경작현황 ⑧농지이용행위 ⑨농지전용행위 (각 추정/결과 2열씩)

**참고**: 이 도구는 위 9대 유형 체크리스트 기능을 의도적으로 구현하지 않음 — 사용자가 "팜필드 입력 후 수정 가능하고 메모앱으로 정리하면 되니 불필요"라고 명시적으로 범위에서 제외함.

## 8. 배포 가이드 (비공개 저장소 호스팅 + 카카오 API 발급) — 9번째 세션 신규

> 이 절은 코드가 아니라 "사용자가 브라우저/카카오 디벨로퍼스 화면에서 직접 클릭해야 하는" 운영 절차임. 다음 세션에서 이 절차를 실제로 수행했는지, 막힌 단계가 있는지 확인할 것.

### 8-1. 비공개 GitHub 저장소 + Cloudflare Pages 호스팅

**왜 이 조합인가**: GitHub Pages는 개인 무료 계정에서 비공개 저장소를 호스팅할 수 없음(Pro 이상 유료 플랜 필요, 공식 확인됨). Cloudflare Pages는 무료 플랜에서도 비공개 저장소 연동을 공식 지원함(Cloudflare 공식 문서로 확인됨). 그래서 소스코드는 비공개로 유지하면서 배포는 Cloudflare Pages로 하는 조합을 씀.

1. GitHub에서 저장소를 **Private**으로 생성(또는 기존 저장소면 Settings → Danger Zone → Change visibility → Private), `field_route_planner.html`을 저장소 루트에 커밋.
2. https://pages.cloudflare.com 접속 → Cloudflare 계정 생성/로그인(무료).
3. "Workers & Pages" → "Create" → "Pages" 탭 → "Connect to Git" 선택.
4. GitHub 연결 화면에서 **"Only select repositories"**를 골라 방금 만든 저장소만 체크 — 비공개 저장소라도 정상적으로 연동되고, 별도 유료 전환이 필요 없음.
5. 빌드 설정 화면: Framework preset = **None**, Build command = **비워둠**, Build output directory = **`/`**(html이 저장소 최상위에 있는 경우).
6. "Save and Deploy" → 배포 완료되면 `프로젝트이름.pages.dev` 형태의 **고정 도메인**이 생김. 이 도메인이 앞으로 juso.go.kr·카카오 키 등록에 쓸 "고정 배포 도메인"임(주소 API들은 대부분 도메인 단위로 키를 검증하므로, 이 도메인이 먼저 확보돼야 다음 단계인 8-2가 가능함).
7. 이후 GitHub에 push할 때마다 자동으로 재배포됨(기본 브랜치 main 기준).

### 8-2. 카카오 API 키 2개 발급 (2026-07-21 정책 변경 반영, 확인 시점: 2026년 8월)

카카오가 2026-07-21부로 카카오맵/로컬 API 이용 절차를 바꿈: 예전엔 "추가 기능 신청 → 관리자 심사"가 필요했는데, 이제는 앱 관리 페이지에서 즉시 활성화 가능. 그리고 **"무료 쿼터는 개발자 계정 기준 가장 먼저 활성화한 앱 1개에만 제공, 두 번째 앱부터는 비즈월렛 연결한 유료 전환"**이라는 원칙이 카카오 공식 공지로 명문화됨 — 8번째 세션에서 이미 이 방향(계정 2개, 계정마다 앱 1개씩)으로 짜둔 `KAKAO_APP_KEYS` 구조가 이 정책과 정확히 맞음. 전략 변경 없이 아래 절차를 **계정 A, 계정 B로 두 번 반복**하면 됨.

1. 카카오 계정으로 https://developers.kakao.com/console/app 로그인(계정 A).
2. "애플리케이션 추가하기" → 앱 이름 아무거나 입력 후 생성.
3. 생성된 앱 클릭 → "플랫폼" 메뉴 → "Web 플랫폼 등록" → 8-1에서 확보한 Cloudflare Pages 고정 도메인(`https://프로젝트이름.pages.dev`)을 사이트 도메인으로 등록. *(호스팅을 먼저 해야 하는 이유가 이 단계 때문임.)*
4. "카카오맵" 메뉴(또는 "제품 설정 > 카카오맵")에서 "카카오맵 API 활성화" 클릭 — 2026-07-21부터는 심사 없이 이 화면에서 바로 켜짐. **⚠️ 이 계정에서 가장 먼저 활성화하는 앱만 무료 쿼터를 받으므로, 계정 A에는 앱을 1개만 만들고 그 앱에서만 활성화할 것.**
5. "앱 키" 메뉴에서 **JavaScript 키**를 복사(REST API 키 아님 — 이 프로젝트는 SDK 방식이라 JS 키를 씀).
6. 계정 B로도 2~5번을 반복해서 두 번째 키를 받음.
7. 계정 A 키를 `KAKAO_APP_KEYS` 배열의 첫 번째 자리, 계정 B 키를 두 번째 자리에 채워넣기(443번째 줄, `field_route_planner.html`).

**확인해볼 점**: 로컬 API 무료 쿼터(현재 앱당 일 10만 건으로 알려짐)는 이번 정책 변경 공지에 "기존 제공량 유지"로 안내돼 있지만, 카카오 측이 "운영 정책 및 사용 현황에 따라 제공 기준이 바뀔 수 있다"고 명시해뒀음 — 실제 발급 시 앱 관리 페이지의 "무료 제공 쿼터" 화면에서 최신 숫자를 한 번 더 확인할 것.

### 8-3. 카카오 사용량을 여러 조사원이 함께 보기 — Firebase Realtime Database로 구현됨 (10번째 세션)

현재 구조(`frp_kakao_quota_0`/`frp_kakao_quota_1`)는 각자 브라우저의 localStorage에만 저장됨 — "내 브라우저가 오늘 몇 번 불렀는지"만 알 수 있고, 다른 조사원이 같은 키로 얼마나 썼는지는 원천적으로 알 방법이 없음(로컬 저장소는 브라우저 밖을 볼 수 없음). 9번째 세션에서 비교한 세 후보(Firebase Realtime DB / Cloudflare Pages Functions+KV / Google Apps Script+스프레드시트) 중, 사용자가 "서버 코드 없는 단일 HTML" 구조를 그대로 유지할 수 있는 **Firebase Realtime Database**를 선택 — 10번째 세션에서 실제 코드로 구현함(아래 8-4절).

### 8-4. Firebase 설정 방법 (실제 구현 완료, 값 채우기만 남음)

1. https://console.firebase.google.com → 프로젝트 추가 (무료 Spark 요금제로 충분).
2. 왼쪽 메뉴 "빌드 > Realtime Database" → 데이터베이스 만들기 (리전은 아무 곳이나 무방, 일단 "테스트 모드"로 만들어도 되지만 반드시 3번 규칙으로 교체할 것).
3. Realtime Database > "규칙" 탭에서 기존 규칙을 아래로 완전히 교체 (카운트를 1씩 늘리는 쓰기만 허용, 임의의 값으로 덮어쓰거나 줄이거나 지우는 건 차단):
   ```json
   {
     "rules": {
       "kakaoQuotaShared": {
         "$keyIdx": {
           "$date": {
             ".read": true,
             ".write": "newData.isNumber() && newData.val() >= 0 && (!data.exists() || newData.val() === data.val() + 1)"
           }
         }
       }
     }
   }
   ```
4. 프로젝트 설정(⚙) > 일반 탭 > "내 앱" 섹션에서 웹 앱 추가(`</>` 아이콘), 앱 닉네임은 아무거나.
5. 화면에 나오는 `firebaseConfig` 객체를 그대로 복사해서 `field_route_planner.html`의 `FIREBASE_CONFIG` 상수(카카오 키 상수 바로 아래)에 붙여넣기. `apiKey`가 소스코드에 그대로 노출돼도 괜찮음 — 카카오 JS 키와 마찬가지로 실제 접근 제어는 키 값이 아니라 3번의 Realtime Database 보안 규칙이 담당함.
6. `FIREBASE_CONFIG`를 채우지 않으면(플레이스홀더 그대로면) 이 기능 전체가 조용히 꺼지고 기존처럼 "이 브라우저 기준" 표시만 동작함 — 필수 아님, 언제든 나중에 채워도 됨.

**동작 방식(코드 리뷰로 확인, 실제 브라우저 호출은 미검증 — 아래 5절 참고)**: 카카오 지오코딩이 성공할 때마다 기존 로컬 카운터(`bumpKakaoQuota`)에 더해 `bumpSharedKakaoQuota`가 Firebase의 `kakaoQuotaShared/{키번호}/{날짜}` 경로를 `transaction()`으로 1씩 원자적으로 증가시킴. 동시에 `subscribeSharedKakaoQuota`가 같은 경로를 실시간 구독하고 있어서, 다른 조사원이 호출해도 내 화면의 "전체 조사원 합산" 숫자가 자동으로 갱신됨. 파이어베이스 SDK는 카카오 SDK와 같은 패턴(정적 `<script>` 태그 대신 필요할 때만 동적으로 로드)으로 붙여서, 설정 안 한 사용자에게는 불필요한 스크립트 로드가 아예 일어나지 않음.

**Cloudflare는 어떻게 됐나**: 호스팅은 원래 계획(8-1절)대로 Cloudflare Pages를 그대로 씀 — 이건 "카운터를 어디에 만들지"의 선택지가 아니라 "이 HTML 파일을 어디에 올릴지"의 선택지라 별도 구현이 필요 없음(정적 파일 그대로 배포). 사용량 공유 카운터의 백엔드로 Cloudflare Pages Functions+KV까지 병행 구현하는 건 이번 세션엔 하지 않음 — 이미 Firebase 하나로 "여러 사용자가 같은 숫자 보기" 요구사항은 충족되고, 같은 목적의 백엔드를 두 개 만들 필요는 없다고 판단함. 필요해지면(예: Firebase 무료 한도 초과 등) 다음 세션에 추가 검토 가능.

## 9. 파일 위치

- `새 폴더/index (9).html` — 실제 도구 (11번째 세션 작업본)
- `새 폴더/HANDOFF.md` — 이 문서

다음 세션 시작할 때는 사용자가 `field_route_planner.html`과 이 `HANDOFF.md`를 다시 첨부해줘야 이어서 작업 가능함 (세션 간 파일이 자동으로 유지되지 않음).
